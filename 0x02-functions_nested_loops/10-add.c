#include "main.h"

/**
 * add - Function that adds two integers and returns the result.
 * @a : first number.
 * @b : second numer.
 * Return: sum of first and second numbers.
 */

int add(int a, int b)
{
	return (a + b);
}
