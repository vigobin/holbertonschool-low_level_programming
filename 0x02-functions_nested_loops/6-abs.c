#include "main.h"

/**
 * _abs - Function that computes the absolute value of an integer.
 * @n : integer input.
 * Return: 0
 */

int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
